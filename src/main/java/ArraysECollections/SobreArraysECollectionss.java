package ArraysECollections;

public class SobreArraysECollectionss {

    /**
     * São estruturas que agrupam dados em uma única estrutura: lista, pilha, conjuntos, fila, estruturas ordenadas, não ordenadas, etc.
     *
     * ARRAY - estrutura estátíca, tamanho fixo (quando é criado vc fixa um tamanho).
     * Ele é homogênio, quando define o seu tipo, então tudo será do mesmo tipo: inteiro, string, etc.
     * É indexado: para acessar cada elemento, deve ser pelo indice com valor inteiro a partir do 0.
     * É um objeto: tem atributos e comportamento.
     * Ex: int [] a = new int [6]; um array do tipo inteiro com 6 elementos; para inserir é só selecionar o indice: a[2] = 8;
     * Ex: ARRAY DENTRO DE ARRAY(MATRIZ)
     *       double [] [] a = new double [3] [3];
     *
     * FOREACH - laço de repetição especializado na iteração de arrays, projetado para percorrer todos os elementos de um array.
     *
     * HASHCODE - função que retorna um inteiro. Tem execução rápida, com objetivo de separar tudo o que não é igual daquilo que pode ser igual,
     * formando subconjuntos do conjunto inteiro. Faz buscas rápidas para depois rodar o equals. Geralmente é executado antes do equals, para faciliar
     * a sua execução. Utilizado em grandes estruturas de dados.
     *
     * EQUALS - método de comparação de igualdade entre objetos. para implementa-lo vc deve definir como vc quer comparar a igualdade
     * dos objetos. retorna um boolean: true ou false. Geralmente anda em conjunto com o hashcode para executar mais rápido.
     *
     * COLLECTIONS - estrturas e formas de organização de dados, tem tamanhos variados, sem tamanho determinado, podendo crescer de acordo com o tempo. Os dados podem ser diferentes.
     * Nas boas práticas o ideal são criações de um tipo de dado, trabalhar de forma homogênea. As collections não aderem os tipos primitivos.Alguns tipos de collections:
     * SET - não ordenado(padrão); não indexado; não aceita repetição.(conj com vários valores dentro)
     * LIST - indexada; aceita repetição. Parecido com o Array. (um estrtura de lista)
     * MAP - chave/valor; chave nao aceita repetição; valor aceita repetição. (um estrutura com 2 colunas) Junção
     * QUEUE - implementa fila; First in First out(FIFO) (Fila)
     * STACK - implementa pilha(stack); last in/First out (LIFO) (Pilha)
     *
     *
     * WRAPPER - coleções dos tipos primitivos.
     *
     *
     *
     *
     * .*/

}
